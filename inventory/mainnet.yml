---
# maintains testnet inventory grouped by project names
all:
  vars:
    env: mainnet
  children:
    celestia:
      hosts:
        bridge.celestia.mainnet.encapsulate.xyz:
          type: bridge
